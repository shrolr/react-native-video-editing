import { StyleSheet } from "react-native";
let AppStyles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 24,
        backgroundColor: '#fff',
    },
    title: {
        fontSize: 20,
        color: '#fff'
    },
});

export default AppStyles